export interface ZaxConfig {
  webserviceUrl: string;
  notificationServiceUrl: string;
  streamApiKey: string;
  streamAppId: string;
  cloudfrontUrl: string;
  stripePublishableKey: string;
  googleMapsApiKey: string;
}