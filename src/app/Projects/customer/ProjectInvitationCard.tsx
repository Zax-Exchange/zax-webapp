import React from "react";
import { ProjectInvitation } from "../../../generated/graphql";

const ProjectInvitationCard = ({
  invitation,
}: {
  invitation: ProjectInvitation;
}) => {
  return <div>ProjectInvitationCard</div>;
};

export default ProjectInvitationCard;
